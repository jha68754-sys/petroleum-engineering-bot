[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[deploy.env]
LOG_LEVEL = "INFO"
POLLING_TIMEOUT = "30"
STARTUP_DELAY = "45"
